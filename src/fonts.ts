import { Fira_Code, Inter } from 'next/font/google'

export const inter = Inter({
  weight: ['400', '500'],
  subsets: ['latin'],
})

export const firaCode = Fira_Code({
  weight: ['400'],
  subsets: ['latin'],
})
